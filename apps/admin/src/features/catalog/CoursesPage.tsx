'use client';
import Link from 'next/link';
import { useCallback, useEffect, useState } from 'react';
import { CatalogDialog, CatalogError, CatalogLoading } from './CatalogDialog';
import { deleteCourse, listAdminCourses, publishCourse, unpublishCourse } from './catalog-client';
import type { CatalogMutationError, CourseRecord, PageMeta } from './catalog.types';
type Pending = { kind: 'publish' | 'unpublish' | 'delete'; row: CourseRecord } | null;
export function CoursesPage() { const [rows, setRows] = useState<CourseRecord[]>([]); const [meta, setMeta] = useState<PageMeta | null>(null); const [q, setQ] = useState(''); const [status, setStatus] = useState(''); const [page, setPage] = useState(1); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [pending, setPending] = useState<Pending>(null); const [confirm, setConfirm] = useState(''); const load = useCallback(() => { setLoading(true); setError(''); void listAdminCourses({ q, status, page, limit: 12 }).then((result) => { setRows(result.data); setMeta(result.meta); }).catch((cause: unknown) => setError(cause instanceof Error ? cause.message : 'Unable to load courses')).finally(() => setLoading(false)); }, [page, q, status]); useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, [load]); async function action() { if (!pending || (pending.kind === 'delete' && confirm !== pending.row.name)) return; try { if (pending.kind === 'publish') await publishCourse(pending.row.id, pending.row.updatedAt); if (pending.kind === 'unpublish') await unpublishCourse(pending.row.id, pending.row.updatedAt); if (pending.kind === 'delete') await deleteCourse(pending.row.id, pending.row.updatedAt); setSuccess(pending.kind === 'publish' ? 'Course published.' : pending.kind === 'unpublish' ? 'Course unpublished.' : 'Course deleted.'); setPending(null); setConfirm(''); void load(); } catch (cause: unknown) { const typed = cause as Partial<CatalogMutationError>; setError(typed.message ?? 'Course action failed'); setPending(null); setConfirm(''); } } return <section aria-labelledby="courses-heading" className="mx-auto max-w-[1180px]"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#828B9B]">Catalog discovery</p><h2 id="courses-heading" className="mt-2 text-3xl font-semibold">Courses</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#667085]">Manage course core data, verified country availability, and controlled editorial sections.</p></div><Link href="/courses/new" className="rounded-xl bg-[#1657CF] px-5 py-3 text-center text-sm font-semibold text-white">Create course</Link></div>{success ? <p role="status" className="mt-6 rounded-xl bg-[#E9F8F0] px-4 py-3 text-sm font-semibold text-[#18794E]">{success}</p> : null}{error ? <CatalogError message={error} onRetry={load} /> : null}<div className="mt-8 grid gap-4 sm:grid-cols-[1fr_180px_auto]"><label className="text-sm font-semibold">Search<input className="mt-2 w-full rounded-xl border border-[#D9E0EA] px-4 py-3 font-normal" value={q} onChange={(event) => { setQ(event.target.value); setPage(1); }} placeholder="Name, slug, or code" /></label><label className="text-sm font-semibold">Status<select className="mt-2 w-full rounded-xl border border-[#D9E0EA] px-4 py-3 font-normal" value={status} onChange={(event) => { setStatus(event.target.value); setPage(1); }}><option value="">All</option><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option></select></label><button type="button" onClick={() => { setQ(''); setStatus(''); setPage(1); }} className="self-end rounded-xl border border-[#D9E0EA] px-4 py-3 text-sm font-semibold">Clear</button></div>{loading ? <CatalogLoading label="Loading courses…" /> : rows.length ? <div className="mt-5 divide-y divide-[#E8ECF3] overflow-hidden rounded-2xl border border-[#E8ECF3] bg-white">{rows.map((row) => <article key={row.id} className="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-lg font-semibold">{row.name}</h3><span className="rounded-full bg-[#F0F4FA] px-2.5 py-1 text-xs font-bold">{row.status}</span></div><p className="mt-1 text-sm text-[#828B9B]">{row.subject.name} · {row.courseLevel.name} · /{row.slug}</p></div><div className="flex flex-wrap gap-2"><Link href={`/courses/${row.id}`} className="rounded-lg border border-[#D9E0EA] px-3 py-2 text-sm font-semibold">Edit</Link>{row.status === 'PUBLISHED' ? <button type="button" onClick={() => setPending({ kind: 'unpublish', row })} className="rounded-lg border border-[#D9E0EA] px-3 py-2 text-sm font-semibold">Unpublish</button> : <button type="button" onClick={() => setPending({ kind: 'publish', row })} className="rounded-lg bg-[#1657CF] px-3 py-2 text-sm font-semibold text-white">Publish</button>}<button type="button" onClick={() => setPending({ kind: 'delete', row })} className="rounded-lg border border-[#F2C5C5] px-3 py-2 text-sm font-semibold text-[#B42318]">Delete</button></div></article>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-[#CBD5E4] bg-white p-12 text-center"><h3 className="text-lg font-semibold">No courses found</h3><p className="mt-2 text-sm text-[#667085]">Create a course or adjust the filters.</p></div>}{meta && meta.totalPages > 1 ? <div className="mt-5 flex items-center justify-between"><button type="button" disabled={page <= 1} onClick={() => setPage((value) => value - 1)} className="rounded-lg border border-[#D9E0EA] px-4 py-2 text-sm font-semibold disabled:opacity-40">Previous</button><span className="text-sm text-[#667085]">Page {meta.page} of {meta.totalPages}</span><button type="button" disabled={page >= meta.totalPages} onClick={() => setPage((value) => value + 1)} className="rounded-lg border border-[#D9E0EA] px-4 py-2 text-sm font-semibold disabled:opacity-40">Next</button></div> : null}{pending ? <CatalogDialog title={pending.kind === 'delete' ? 'Delete course?' : `${pending.kind === 'publish' ? 'Publish' : 'Unpublish'} course?`} description={pending.kind === 'delete' ? `Type ${pending.row.name} to confirm soft deletion.` : 'This changes public visibility and is recorded in the audit log.'} onClose={() => { setPending(null); setConfirm(''); }}><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-end"><div className="flex-1">{pending.kind === 'delete' ? <label className="text-sm font-semibold">Course name<input aria-label="Confirm course name" className="mt-2 w-full rounded-lg border border-[#D9E0EA] px-3 py-2 text-sm font-normal" value={confirm} onChange={(event) => setConfirm(event.target.value)} placeholder={pending.row.name} /></label> : null}</div><button type="button" className="rounded-lg border border-[#D9E0EA] px-4 py-2 text-sm font-semibold" onClick={() => { setPending(null); setConfirm(''); }}>Cancel</button><button type="button" disabled={pending.kind === 'delete' && confirm !== pending.row.name} className="rounded-lg bg-[#1657CF] px-4 py-2 text-sm font-semibold text-white disabled:opacity-40" onClick={() => void action()}>Confirm</button></div></CatalogDialog> : null}</section>; }
