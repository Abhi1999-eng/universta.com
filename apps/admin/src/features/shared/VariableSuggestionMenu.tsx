import type { DynamicVariable } from './variable-autocomplete';

export function VariableSuggestionMenu({ id, variables, activeIndex, onSelect }: { id?: string; variables: DynamicVariable[]; activeIndex: number; onSelect: (variable: DynamicVariable) => void }) {
  return <div id={id} role="listbox" aria-label="Available dynamic variables" className="absolute left-0 right-0 top-full z-50 mt-1 max-h-56 overflow-y-auto rounded-xl border border-[#C9D7F2] bg-white p-2 shadow-xl sm:right-auto sm:w-80">{variables.length ? variables.map((variable, index) => <button key={variable.key} type="button" role="option" aria-selected={activeIndex === index} onMouseDown={(event) => event.preventDefault()} onClick={() => onSelect(variable)} className={`block w-full rounded-lg px-3 py-2 text-left text-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-[#1657CF] ${activeIndex === index ? 'bg-[#EAF1FF] text-[#0D3F9E]' : 'text-[#344054] hover:bg-[#F7F9FC]'}`}><span className="font-semibold">{variable.label}</span><span className="ml-2 text-xs text-[#667085]">{`{${variable.key}}`}</span></button>) : <p className="px-3 py-2 text-sm text-[#667085]">No matching variables.</p>}</div>;
}
