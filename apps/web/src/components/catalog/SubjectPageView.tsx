import Link from 'next/link';
import type { SubjectDetail } from '@/lib/catalog';
export function SubjectPageView({ subject }: { subject: SubjectDetail }) {
  return <main><header className="site-header"><div className="shell header-inner"><Link className="brand" href="/subjects">universta<span>.</span></Link><nav aria-label="Primary navigation"><Link href="/subjects">Subjects</Link><Link href="/courses">Courses</Link><Link href="/countries">Countries</Link></nav></div></header><section className="detail-hero"><div className="shell detail-hero-grid"><div><Link className="back-link" href="/subjects">← All subjects</Link><p className="eyebrow">Subject pathway</p><h1>{subject.name}</h1><p className="hero-copy">{subject.shortDescription ?? 'Explore published courses, specialisations, and verified study destinations.'}</p></div><div className="hero-card">{subject.heroMedia ? <img src={subject.heroMedia.url} alt={subject.heroMedia.alt ?? subject.name} /> : <div className="hero-placeholder" aria-hidden="true">{subject.name.slice(0, 1)}</div>}<span>{subject.publishedCourseCount} published courses</span><small>{subject.availableCountryCount} available countries</small></div></div></section><div className="detail-content shell"><div className="detail-main"><section className="editorial-section"><p className="eyebrow">Overview</p><h2>Build your next academic direction.</h2><p>{subject.overview ?? 'This subject overview will be expanded from verified editorial content as it becomes available.'}</p></section>{subject.subSubjects.length ? <section className="editorial-section" aria-labelledby="specialisations-heading"><p className="eyebrow">Sub-Subjects</p><h2 id="specialisations-heading">Explore specialisations</h2><div className="catalog-card-grid">{subject.subSubjects.map((child) => <article className="catalog-card compact" key={child.id}><div className="catalog-card-body"><h3>{child.name}</h3><p>{child.shortDescription ?? 'Published specialisation within this subject.'}</p><Link className="card-link" href={`/courses?subject=${encodeURIComponent(subject.slug)}&subSubject=${encodeURIComponent(child.slug)}`}>View courses <span aria-hidden="true">→</span></Link></div></article>)}</div></section> : null}{subject.courseCountsByLevel.length ? <section className="editorial-section" aria-labelledby="levels-heading"><p className="eyebrow">Course levels</p><h2 id="levels-heading">Courses by level</h2><div className="structured-grid">{subject.courseCountsByLevel.map((item) => <div className="structured-fact" key={item.level.id}><strong>{item.count}</strong><span>{item.level.name}</span></div>)}</div></section> : null}{subject.featuredCourses.length ? <section className="editorial-section" aria-labelledby="featured-courses-heading"><p className="eyebrow">Featured courses</p><h2 id="featured-courses-heading">Start exploring</h2><div className="catalog-card-grid">{subject.featuredCourses.map((course) => <article className="catalog-card compact" key={course.id}><div className="catalog-card-body"><h3>{course.name}</h3><p>{course.shortDescription ?? course.qualificationName ?? 'Published course'}</p><Link className="card-link" href={`/courses/${course.slug}`}>View course <span aria-hidden="true">→</span></Link></div></article>)}</div></section> : null}</div><aside className="facts-panel"><p className="eyebrow">Published data</p><h2>Subject snapshot</h2><div className="fact"><span>Courses</span><strong>{subject.publishedCourseCount}</strong></div><div className="fact"><span>Specialisations</span><strong>{subject.publishedSubSubjectCount}</strong></div><div className="fact"><span>Countries</span><strong>{subject.availableCountryCount}</strong></div><Link className="button" href={`/courses?subject=${encodeURIComponent(subject.slug)}`}>Browse courses</Link></aside></div><section className="consultation-band"><div className="shell consultation-inner"><div><p className="eyebrow">Ready for a conversation?</p><h2>Turn a subject into a plan.</h2><p>Use published course data as a starting point and verify important decisions with official sources.</p></div><Link className="button light" href="/courses">Explore courses</Link></div></section><footer className="site-footer"><div className="shell footer-grid"><div><Link className="brand" href="/subjects">universta<span>.</span></Link><p>Structured, source-aware study guidance.</p></div><p className="footer-note">Information may vary by institution, programme, applicant, and policy.</p></div></footer></main>;
}
