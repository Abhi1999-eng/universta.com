import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Testimonials | Universta', alternates: { canonical: '/testimonials' } }; export default async function TestimonialsPage() { let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('testimonials'); } catch {} return <PhaseListing resource="testimonials" rows={result.data} meta={result.meta} search={false} />; }
