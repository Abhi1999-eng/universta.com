import { notFound } from 'next/navigation'; import { PhaseDetail } from '@/components/phase1/PhaseOneViews'; import { phaseDetail } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export default async function ConsultantPage({ params }: { params: Promise<{ slug: string }> }) { try { return <PhaseDetail resource="consultants" row={await phaseDetail<any>('consultants', (await params).slug)} />; } catch { notFound(); } }
