import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Events | Universta', alternates: { canonical: '/events' } }; export default async function EventsPage() { let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('events'); } catch {} return <PhaseListing resource="events" rows={result.data} meta={result.meta} search={false} />; }
