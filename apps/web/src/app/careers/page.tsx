import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Careers | Universta', alternates: { canonical: '/careers' } }; export default async function CareersPage() { let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('jobs'); } catch {} return <PhaseListing resource="jobs" rows={result.data} meta={result.meta} search={false} />; }
