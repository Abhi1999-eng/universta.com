import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Success stories | Universta', alternates: { canonical: '/success-stories' } }; export default async function StoriesPage() { let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('success-stories'); } catch {} return <PhaseListing resource="success-stories" rows={result.data} meta={result.meta} search={false} />; }
