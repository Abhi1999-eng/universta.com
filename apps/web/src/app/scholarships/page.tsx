import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Scholarships | Universta', alternates: { canonical: '/scholarships' } }; export default async function ScholarshipsPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const raw = await searchParams; const params = Object.fromEntries(Object.entries(raw).flatMap(([key, value]) => typeof value === 'string' && value ? [[key, value]] : [])); let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('scholarships', params); } catch {} return <PhaseListing resource="scholarships" rows={result.data} meta={result.meta} />; }
