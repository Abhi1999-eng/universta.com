import { CompareView } from '@/components/phase1/CompareView'; import { phaseCompare } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Compare consultants | Universta', robots: { index: false, follow: true }, alternates: { canonical: '/compare/consultants' } }; export default async function CompareConsultants({ searchParams }: { searchParams: Promise<{ items?: string }> }) { const items = ((await searchParams).items ?? '').split(',').filter(Boolean).slice(0, 3); let result = { items: [], invalid: [] }; try { result = await phaseCompare<any>('consultants', items); } catch {} return <CompareView type="consultants" result={result} items={items} />; }
