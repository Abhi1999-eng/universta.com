import { CompareView } from '@/components/phase1/CompareView'; import { phaseCompare } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Compare university courses | Universta', robots: { index: false, follow: true }, alternates: { canonical: '/compare/courses' } }; export default async function CompareCourses({ searchParams }: { searchParams: Promise<{ items?: string }> }) { const items = ((await searchParams).items ?? '').split(',').filter(Boolean).slice(0, 3); let result = { items: [], invalid: [] }; try { result = await phaseCompare<any>('courses', items); } catch {} return <CompareView type="courses" result={result} items={items} />; }
