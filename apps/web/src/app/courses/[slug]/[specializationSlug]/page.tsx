import type { Metadata } from 'next'; import { CourseDiscovery } from '@/components/phase1/CourseDiscovery'; export const dynamic = 'force-dynamic'; export async function generateMetadata({ params }: { params: Promise<{ slug: string; specializationSlug: string }> }): Promise<Metadata> { const p = await params; return { title: 'Course discovery | Universta', alternates: { canonical: `/courses/${p.slug}/${p.specializationSlug}` }, robots: { index: false, follow: true } }; } export default async function CourseSubjectSpecializationPage({ params }: { params: Promise<{ slug: string; specializationSlug: string }> }) { const p = await params; return <CourseDiscovery filters={{ subject: p.slug, subSubject: p.specializationSlug }} />; }
