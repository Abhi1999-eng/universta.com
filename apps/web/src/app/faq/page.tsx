import { EditorialPage } from '@/components/phase1/EditorialPage'; import { phasePage } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Frequently asked questions | Universta', alternates: { canonical: '/faq' } }; export default async function FaqPage() { let page: any = null; try { page = await phasePage<any>('faq'); } catch {} return <EditorialPage page={page} fallbackTitle="Frequently asked questions" fallbackDescription="Find published answers about using Universta's study abroad information." />; }
