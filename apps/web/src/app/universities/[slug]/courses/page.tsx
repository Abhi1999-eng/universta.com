import { notFound } from 'next/navigation'; import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseUniversityCourses } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export default async function UniversityCoursesPage({ params }: { params: Promise<{ slug: string }> }) { const value = await params; try { const result: any = await phaseUniversityCourses(value.slug); return <PhaseListing resource="courses" title={`${result.university?.name ?? 'University'} courses`} basePath={`/universities/${value.slug}/courses`} rows={result.data ?? []} meta={result.meta} search={false} />; } catch { notFound(); } }
