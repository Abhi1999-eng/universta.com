import { notFound } from 'next/navigation'; import { UniversityDetail } from '@/components/phase1/PhaseOneViews'; import { phaseDetail } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export default async function UniversityPage({ params }: { params: Promise<{ slug: string }> }) { try { return <UniversityDetail row={await phaseDetail<any>('universities', (await params).slug)} />; } catch { notFound(); } }
