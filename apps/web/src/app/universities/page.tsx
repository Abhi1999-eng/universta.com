import { PhaseListing } from '@/components/phase1/PhaseOneViews'; import { phaseList } from '@/lib/phase1'; export const dynamic = 'force-dynamic'; export const metadata = { title: 'Universities | Universta', alternates: { canonical: '/universities' } }; export default async function UniversitiesPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const raw = await searchParams; const params = Object.fromEntries(Object.entries(raw).flatMap(([key, value]) => typeof value === 'string' && value ? [[key, value]] : [])); let result = { data: [] as any[], meta: null }; try { result = await phaseList<any>('universities', params); } catch {} return <PhaseListing resource="universities" rows={result.data} meta={result.meta} />; }
